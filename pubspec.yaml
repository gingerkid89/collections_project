name: collection_app
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any
  cupertino_icons: ^1.0.2
  provider: ^6.1.1
  shared_preferences: ^2.2.2  # ← Wieder hinzufügen
  image_picker: ^1.0.4
  path_provider: ^2.1.1
  path: ^1.8.3
  flutter_map: ^6.0.1
  latlong2: ^0.9.1
  url_launcher: ^6.2.2
  google_sign_in: ^6.1.6
  sign_in_with_apple: ^6.1.1
  flutter_secure_storage: ^9.0.0
  crypto: ^3.0.3
  flutter_local_notifications: ^17.0.0
  permission_handler: ^11.0.1
  geolocator: ^12.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true